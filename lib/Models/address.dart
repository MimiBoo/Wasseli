class Address {
  String placeFormattedAddress;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  Address({this.placeId, this.placeFormattedAddress, this.placeName, this.latitude, this.longitude});
}
